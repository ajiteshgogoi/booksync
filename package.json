{
  "name": "booksync",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "preview": "cd client && npm run preview",
    "dev": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
